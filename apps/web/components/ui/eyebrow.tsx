export function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="mb-3.5 font-mono text-[11px] uppercase tracking-[0.16em] text-rust">
      {children}
    </p>
  );
}
